/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['dvd6ljcj7w3pj.cloudfront.net', 'tailwindui.com', 'images.unsplash.com'], 
  },
};

export default nextConfig;
